# 交接文档(新对话接手)

> 用途:给下一轮对话快速上手的项目现状速览。新对话先读本文件 + 定位文档,再决定做什么。
> 更新:2026-08-31 · 当前 master 领先 `18b5948` 两个 commit:可见性过滤增强(`56f31e4`)、实时闭环反馈:变更可读化+点击生效报告(待提交)

---

## 0. 项目一句话

**Agent World MCP**:把网页实时翻译成"原生网页世界"(编号图纸 + 增量变更流 + 状态卡),以 17 个 `world_*` MCP 工具暴露给任何 AI agent。内核在 `agent-runtime-extension-v1.1-blueprint/`(注入 JS),通道在 `agent-world-mcp/server.py`。

## 1. ⚠️ 术语硬性约定(不可违反)

- **"世界模型"一词已正式弃用**(与 AI 领域 World Model 冲突),一律改用 **"原生网页世界"**。
- 代码里 `world_*` 工具名、`window.agentWorld`、`world_id`、`_worlds` 注册表**保留不动**(零破坏)。
- 依据:[WebMCP 对比与项目定位.md](./WebMCP%20对比与项目定位.md)(含术语声明 + 项目时间线:2026 年 5 月初独立提出,早于 WebMCP)。

## 2. 目录速览(monorepo 根 = git 仓库根)

```
agent-runtime-extension-v1.1-blueprint/   # 注入内核(JS 源码 + all-in-one.js 合并版)
    ├── content/observer.js               # MutationObserver(累积式防抖+maxWait,已修饿死)
    ├── content/runtime.js                # AgentRuntime 核心调度
    ├── engine/{scanner,query,topology,...}.js
    ├── scripts/build_all_in_one.py       # 改内核源码后必须重跑,生成 all-in-one.js
    └── all-in-one.js                     # server.py 注入用的合并版
agent-world-mcp/
    ├── server.py                         # MCP 服务器(17 工具)
    ├── probe_site.py / probe_fill.py     # 通用站点/填表探针(新站点体检)
    ├── test_official/click/fill/action_layer/enhancements/cdp/...  # 测试脚本
    ├── profiles/  screenshots/
skills/agent-world/SKILL.md               # Agent World 技能包(Playbook)
test_fixtures/dyn.html / ipi_matrix.html  # 本地测试夹具
原生网页世界-IPI防御机制深度分析备忘录.md    # 安全研究入口(含实测攻防矩阵)
WebMCP 对比与项目定位.md                   # 定位/术语/路线图
Agent Web 操作技能包-整体设计.md           # 四层能力设计
Agent Web 操作实战经验手册.md              # 实战踩坑经验
```

## 3. 已完成的关键工作(近 6 个 commit)

| commit | 内容 |
|---|---|
| `fb34916` | 观察器饿死修复(捕获率 28%→98%)、anomaly 口径修复、fill 覆盖层验证、测试脚本可移植化、新增 probe_site/probe_fill |
| `688d046` | 术语重构:世界模型→原生网页世界;新增 WebMCP 对比与定位文档 |
| `a7e5dec` | 定位文档补充项目时间线(早于 WebMCP) |
| `388d850` | 移植 PR 行动层增强:CDP 挂载、world_fill type_delay_ms、world_batch_fill(逐字段容错)、world_click 遮挡检测、_build_locator 可见性过滤、world_press native-keyboard、test_enhancements、SKILL.md、dyn.html |
| `18b5948` | CDP 安全决策(保留机制降级场景 + world_eval 闸门)、IPI 防御备忘录 + 攻防矩阵实测、test_cdp.py |

## 4. 当前状态

- master 领先 origin 2 个 commit(**未推送**):`388d850`、`18b5948`
- GitHub 上还有 PR 分支 `feat/action-enhancements-and-skill`(Gemini 3.7 flash 在另一台电脑写的,已移植有价值部分,`test_real_world.py` 因依赖真实 GF 易 flaky 被搁置)
- 工作树干净

## 5. 待办/下一步(按价值排序)

1. ~~**IPI 过滤缺口修补**~~ **已完成(2026-08-31)**:五种伪隐藏(`color:white`/移出视口/`font-size:0`/`text-indent`/`aria-hidden`)已在内核 `engine/visibility.js#isPseudoHidden` 修补,scanner/dialogs 统一接入,observer 补 aria-hidden 监听,动态时序隐藏→移除闭环。测试:`test_ipi_filter.py`(双维度验证,本地夹具)。详见 IPI 备忘录"实测四"。
2. ~~**点击验证闭环**~~ **已完成(2026-08-31,第一阶段)**。路线经实测反馈决定:
   - **变更可读化**:内核变更事件补 `semantic`(remove 也在删除前捕获 name/semantic);server 层 `world_changes` 返回带 `importance` 分级 + `digest` 人话摘要(`{summary, counts, highlights}`)
   - **world_click 带 `effect` 生效报告**:点击前冻结目标空间区域(±200px)→ 点击后轮询区域 diff(有变化即停)→ `{verdict, confidence, why, observed, region_changed}`
   - **关键实测**:重型 SPA(GF)一次点击全页重渲染(新增108/移除851/更新946),全页 diff 是噪声且元素 ID 失效 → 改为**空间区域作用域**因果,真信号(dialog.number-of-passengers)从噪声中分离;负例(点击无副作用元素)正确判 no-change 不误报
   - 测试:`test_change_digest.py`、`test_click_effect.py`(正例+负例)、`test_wait_event.py`(事件驱动)
   - **world_wait 事件驱动已完成**:内核 `AgentRuntime.waitFor()` 注册 waiter,MutationObserver flush 后 checkWaiters 命中即 resolve;server 端 `page.evaluate` Promise await,去掉 0.3s 轮询。实测已满足 0.04s/动态出现 0.03s/消失 0.16s/超时精确。返回带 `driven: event|timeout`
   - **实战验证已完成(2026-08-31,通过线达标 83%≥80% 且 FP=0)**:`validate_closed_loop.py`(配置驱动+truth oracle)+ 夹具 `far_modal.html`/`tabs.html`。**实战发现并修复三个真实问题**:(a) 内核 observer attributeFilter 漏 `open`/`aria-expanded`/`aria-selected` → HTML 原生弹窗(dialog open 属性)变可见但世界不注册,补后 far-modal FN→TP;(b) effect 加全页可见 dialog/menu 兜底扫描(远距弹窗 ±200px 外也能识别);(c) effect 加目标自身状态翻转证据(aria-selected/aria-expanded/checked/class,tabs AM→TP)。方案/结论见 `实时闭环反馈-实战验证方案.md`
   - **fill/press 带 effect 已完成(2026-08-31)**:填表以"值已进入可见输入框"为强证据;按键按类型判定(Escape 关弹窗=全页 dialog 消失、Enter=URL 变化、方向键=目标状态翻转)。复跑体检 100% 全过(TP=7 TN=1,无假成功无假失败)
   - **证据窗"聪明早停"已完成(2026-08-31)**:已见决定性证据(弹窗/URL/状态/值)即返,不再等 0.4s 稳定。普通操作 0.6s→0.2s,持续变化页 1.6s→0.21s。评估结论:不升级纯事件驱动(边际收益小、复杂度升)。effect 带 `evidence` 计时字段
   - **真网站扩展体检已完成**:新增 Wikipedia 搜索联想、GitHub 仓库标签、Stack Overflow 搜索、Amazon 搜索、BBC 头条场景,多轮全过(TP=9 TN=3,100%)。SO 被反爬挡(SKIP,环境问题)
   - **digest 重要性降权已完成(2026-08-31)**:`validate_closed_loop.py --digest` 多真站价值评估发现原 importance 把 button/link/navigation 标 high,重型 SPA 重渲染外壳"假新增"刷屏(噪声29 vs 强信号5)。修复:digest 改用窄口径 `_DIGEST_HIGH_ROLES`(dialog/menu/option/combobox/input),外壳降 medium;**与 effect 宽口径 `_IMPORTANT_ROLES` 分离**(避免展开子菜单类操作被降权)。降权后真信号14 vs 噪声0、压缩比 7.3→14.1、GF digest 第一行即弹窗。评估命令:`py -3.11 validate_closed_loop.py --digest`
   - **digest 改结构化语义摘要(counts+key,无 summary 人话)已完成(2026-08-31)**:采纳"CAD 图纸 004# 圆孔"原则——digest 从 `{summary,counts,highlights}` 改为 `{counts,key}`,key 是高价值**强 ID 引用**(type/id/semantic/name)。agent 拿到 `el_595` 即可 `world_entity` 查详图,无歧义不猜。实测:key 管线闭环成立(有 key 的场景全部可查详图),完整体检 TP=9 TN=3 100%
   - **世界号 + 稳定指纹已完成(2026-08-31)**:① `world_changes` 事件带 `world_id`(标签页 ID),多世界不混淆;② scanner 新增 `computeFingerprint`(semantic+tag+稳定属性+祖先路径,不含易变文本/class/坐标),`world_entities`/`world_entity` 均带 `fingerprint`——同站多次 world_open 指纹可重算一致,`world_entities(fingerprint=...)` 一步定位上次元素(认路记忆,纯内存不落盘)。测试:`test_fingerprint.py`(同站两次进站指纹一致+负例 0 命中)。范围:仅同一次对话内多次进出;跨对话持久化留作扩展(C 方案暂不实现)
3. **动态时序攻击实验**:注入→隐藏窗口内 world_entities 是否捕获(备忘录待补实验)
4. **aria-label/placeholder 属性通道评估**(VEC_10 需 world_entity 查 attributes 确认)
5. **语义摘要 + 重要性加权**("快"的方向,让 agent 少读)
6. **CDP 安全设计独立立项**(连日常浏览器场景,端口鉴权/域白名单/审计)——已降级暂缓,别在主项目做

## 6. 技术要点/坑(实测)

- **运行测试用 `py -3.11`**(系统 Python 3.11 装了 mcp+playwright;`uv run` 是隔离环境没有)
- **cmd 引号坑**:命令里带中文路径/空格文件名,cmd 会转码破坏 → 用 Python subprocess 或 chcp 65001
- **git 中文文件名**:`git add` 中文/带空格文件名用 Python subprocess(直接传 unicode 路径)
- **改内核后**:`py -3.11 scripts/build_all_in_one.py` 重跑 + `node -c all-in-one.js` 语法检查
- **CDP 测试**:`py -3.11 test_cdp.py`(自动起临时独立 Chrome,world_close 后断言浏览器存活 + world_eval 闸门)
- **常规回归**:`test_official`(全链路)、`test_enhancements`(本地夹具,不依赖外网)、`test_action_layer`(GF 行动层)
- **探针**:`py -3.11 probe_site.py <url> [wait_ms] [stabilize_ms]`、`probe_fill.py <url> <目标> <文本>`
- **screen 截图分析**:`dim image read` 在本环境偶发崩溃(整页大图),压到宽≤900 再读

## 7. 安全边界(已定,勿破坏)

- CDP 只支持**独立 profile** 启动的 Chrome;连日常浏览器场景已降级"暂不推荐",安全设计单独立项
- **CDP 会话下 world_eval 已禁用**(IPI 后门防线),测试 `test_cdp.py [3.5]` 断言它
- profile 持久化登录态仅 `world_close` 导出;CDP 连接不导出 storage_state
- 已知限制:每世界 ~200MB 内存;反爬站点(headless)会拦;页面拒绝注入会报错

## 8. 给新对话的提示词(可直接用)

> 你是这个 monorepo 的开发者。先读根目录《交接文档(新对话接手)》和《WebMCP 对比与项目定位.md》,了解项目现状。然后:【在此写你要做的事,比如"修补 IPI 伪隐藏过滤缺口"/"实现点击验证闭环"/"跑新站点探针验证"...】。注意:术语用"原生网页世界"不用"世界模型";改内核后重建 all-in-one.js;测试用 py -3.11。
