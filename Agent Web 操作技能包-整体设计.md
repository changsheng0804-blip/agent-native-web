# Agent Web 操作技能包(整体设计)

> 版本:v1.0 · 2026-08-30
> 定位:AI Agent 操作网页的完整能力组合——不是"一个工具",而是"四层能力 + 一套流程"

---

## 一、核心思想

没有任何单一方案能独立完成"可靠地操作任意网页"。四个能力层各有所长、各有所短,**组合才是完整答案**:

```
┌─────────────────────────────────────────────────────────┐
│                  Agent 网页操作技能包                      │
├───────────┬──────────────┬────────────┬─────────────────┤
│  理解层    │   行动层      │   兜底层    │    扩展形态      │
│ agentWorld│ Playwright   │ 视觉模型   │  真实浏览器连接   │
│ 原生网页世界   │ 原生能力      │ 截图+识图  │  插件/CDP        │
├───────────┼──────────────┼────────────┼─────────────────┤
│ 看:低成本 │ 做:稳定可靠   │ 看:像素级  │ 做:登录态/人工   │
│ 空间/语义 │ 自动等待      │ 图表/图片  │ 验证码/真人确认  │
│ 变更流    │ 错误诊断      │ 布局核对   │                 │
└───────────┴──────────────┴────────────┴─────────────────┘
```

## 二、四层能力清单

### 1. 理解层:agentWorld 原生网页世界 ✅ 已落地验证

| 能力 | 说明 | 状态 |
|---|---|---|
| 世界摘要 | 打开即知"多少元素/多少可交互/什么类型" | ✅ |
| 编号系统 | 每个元素有稳定编号(`el_89`)和语义名字(`combobox.round-trip`) | ✅ |
| 空间语义 | 坐标、网格、上下左右邻居、视觉阅读顺序、空白区域 | ✅ |
| 图纸查询 | 按角色/文本/名字/可交互过滤构件清单 | ✅ |
| 变更日志 | 页面变化增量续读(游标),不重不漏 | ✅ |
| 多页并行 | 独立世界,互不干扰,零桌面打扰 | ✅ |

**短板**:看不见像素(图片文字/图表/验证码);行动层弱(无自动等待/错误诊断)。

### 2. 行动层:Playwright 原生能力 ✅ 已整合(locator 优先)

| 能力 | 说明 |
|---|---|
| 自动等待 | 元素可见/可点击才操作,页面动态也能等 |
| 清晰错误 | "元素被遮挡/不可见/超时"直接告诉你原因 |
| 键盘/鼠标 | 输入、按键、拖拽、hover 全套真实手势 |
| 三层降级 | locator(默认)→ 坐标手势 → JS 注入,`method` 字段标明路径 |

**短板**:对页面"长什么样"无感知;纯 headless 无登录态;信息获取是重型的(反复抓全量)。

### 3. 兜底层:视觉模型 ✅ 已具备(本次验证中多次使用)

| 能力 | 说明 |
|---|---|
| 像素级观察 | 图表、logo、图片文字、验证码内容 |
| 布局核对 | "面板弹出了吗""按钮在哪"——原生网页世界的校验器 |
| 失败诊断 | agentWorld 查询不到/行动无反应时,截图问视觉 |

**短板**:贵(每张截图 1-2K token)、会看错、不能替代结构化数据。

### 4. 扩展形态:真实浏览器连接 ⏳ 设计已明确,待实现

| 能力 | 说明 |
|---|---|
| 登录态 | 用户日常 Chrome 的 cookie 天然可用 |
| 人工介入点 | 验证码/短信/真人确认由用户在浏览器里完成 |
| 增量价值 | 插件(agent-runtime-extension)已具备注入内核,缺 CDP 桥接 |

**现状**:headless MCP 形态无法做登录类任务;**这是唯一缺口**,也是最独特的扩展方向。

## 三、使用决策树(什么场景走哪条路)

```
任务来了
│
├─ 纯抓取/静态页/看文本内容?
│    └─→ webfetch / 原生 Playwright(最轻,不用技能包)
│
├─ 复杂动态页 / 多页对比 / 需要理解页面结构?
│    └─→ 理解层(agentWorld):world_open → layers → entities
│
├─ 需要点击/填表/操作?
│    └─→ 行动层(Playwright):优先用原生网页世界的编号+名字定位
│         ↓ 操作后必做
│         验证:world_changes / world_entities / world_wait
│
├─ 行动无反应 / 需要看像素(图表/图片/布局)?
│    └─→ 兜底层:截图 + 视觉模型 → 修正策略
│
└─ 登录 / 验证码 / 真人确认 / 需要我的账号?
     └─→ 扩展形态:真实浏览器 + 人工介入(待实现)
         当前过渡方案:用户手动完成登录后,再继续
```

## 四、标准操作流程(Playbook)

```
1. 打开世界    world_open(url)          → 拿到摘要(元素/交互/类型)
2. 看结构      world_layers()           → 理解页面有哪些区块
3. 找构件      world_entities(过滤)     → 定位目标(编号+名字+坐标)
4. 操作        world_click(id) / world_fill(id, text)
5. 验证        world_changes(since)     → 操作后页面发生了什么
               world_wait(appear)       → 等预期结果出现
6. 失败分支    world_screenshot → 视觉模型 → 修正定位或换策略
7. 登录分支    涉及账号 → 停止自动化,用户人工完成后继续
```

**关键纪律**:
- 操作后必验证(变更日志是"操作是否生效"的客观证据)
- 行动失败先问视觉,不盲目重试
- 需要登录立刻停,不硬闯

## 五、真实案例复盘(本次验证,Google Flights)

| 步骤 | 走的是哪层 | 结果 |
|---|---|---|
| 打开+摘要 461 元素 | 理解层 | ✅ 一次拿到 |
| 点击乘客按钮(名字驱动) | 理解层定位+行动层手势 | ✅ 面板弹出,变更流记录 |
| 填 Tokyo → 建议列表 | 理解层定位+行动层 setter | ✅ `option.tokyo-japan` 出现 |
| 一次点击"无效" | 兜底层介入 | 截图+视觉确认 → 发现是查询条件错误 |
| fill 无反应 | 兜底层+理解层 | 发现覆盖层副本 → 修复自动切换 |

**复盘结论**:四层缺一不可——没有理解层,定位靠猜;没有行动层,操作不稳;没有兜底层,失败看不出原因。

## 六、当前落地状态

| 组件 | 状态 | 位置 |
|---|---|---|
| 理解层内核(v1.1 蓝图版) | ✅ 已交付 | `agent-runtime-extension-v1.1-blueprint/` |
| 理解层通道(MCP 13 工具) | ✅ 已交付并接入 opencode | `agent-world-mcp/` |
| 行动层(locator 优先整合) | ✅ 已整合:click/fill/press 三层策略(Playwright 原生 → 坐标手势 → JS 注入) | `agent-world-mcp/server.py` |
| 兜底层(视觉) | ✅ 本机可用(vision skill) | `~/.claude/skills/vision` |
| 扩展形态(headful + profile) | ✅ 已落地:`world_open` 支持弹窗 + 持久化登录态,登录/验证码/人工介入闭环打通 | `agent-world-mcp/server.py` |

## 七、下一步建议(按价值排序)

1. ~~行动层整合~~ ✅ **已完成**
2. ~~扩展形态(阶段 1:专用浏览器)~~ ✅ **已完成**:headful 弹窗(人工介入)+ profile 持久化(登录态),登录/验证码场景闭环
3. **技能包 skill 化**:把本决策树+Playbook 做成 opencode 可触发的 skill,Agent 遇到网页任务自动走流程
4. **阶段 2(可选)**:扩展 + chrome.debugger 桥接,操作你日常 Chrome 的已登录会话(人机协同场景,安全设计需单独规划)