# WebMCP 对比与项目定位

> 版本:2026-08-31
> 主题:本项目(Agent World MCP)与 WebMCP 标准的关系、区别、以及由此校准的定位
> 配套:[Agent Web 操作技能包-整体设计.md](./Agent%20Web%20操作技能包-整体设计.md) · [Agent Web 操作实战经验手册.md](./Agent%20Web%20操作实战经验手册.md) · [原生网页世界-IPI防御机制深度分析备忘录.md](./原生网页世界-IPI防御机制深度分析备忘录.md)(安全研究入口)

---

## 〇、术语重定义声明(2026-08-31)

> **项目时间线**:本项目于 **2026 年 5 月初**由作者提出并完善(内核 + MCP 通道 + 行动层),**早于 WebMCP 进入公众视野**。WebMCP(Origin Trial 2026 年开跑)的提出,独立印证了"浏览器 agent 直接抓 DOM/截图慢而不准,需要结构化接口"这一方向的正确性——本项目是**独立先行的同类探索**,而非对 WebMCP 的追随或山寨。

本项目历史上使用"**世界模型**"一词描述其核心能力。该词现在**正式弃用**,原因:

> "World Model / 世界模型"在 AI 领域已是成熟的独立概念(如 Sora、物理世界模拟器——指智能体对环境的内部认知模型),与"网页的结构化可操作表示"含义完全不同,用词冲突会造成读者误判。

**新术语**:

| 旧术语 | 新术语 | 英文 | 说明 |
|---|---|---|---|
| 世界模型 | **原生网页世界** | Native Web World | 描述"为智能体原生构建的网页结构化可操作表示" |

**保留与不变**:
- 代码层 `world` 字样**全部保留**:MCP 工具(`world_open`/`world_entities`/...)、`window.agentWorld`、`world_id`、`_worlds` 注册表——零破坏性改动,已接入的 opencode 配置无需修改
- "世界"概念本身仍成立(见下),弃用的只是"**模型**"这个后缀

**为什么"世界"仍然合适**:智能体打开一个网页,获得的是一个**可查询、可操作、持续更新的结构化空间**——有编号(门牌号)、有名字(构件名)、有坐标、有邻居、有变更流(网页视频)。这不是"对环境的认知模型",而是"网页本身为智能体打开的那个世界"。因此定位表述为:

> **构建智能体直接理解的原生网页世界**——把网页实时翻译成智能体可查询、可操作、持续更新的结构化空间,以 MCP 标准工具暴露给任何 AI agent。

后续所有文档一律使用"原生网页世界",不再出现"世界模型"。

---

## 一、WebMCP 是什么

**WebMCP**(Web Model Context Protocol)是一个**Web 标准提案**(W3C 孵化,Chrome/Edge 团队联合推动,Origin Trial 已开跑,Chrome 149 起可用)。

一句话:**让网站主动为 AI agent 声明"我能做什么"**。

- 网站开发者用 `registerTool()`(命令式 JS API)或 HTML 表单注解(声明式)注册工具,带 JSON Schema 和自然语言描述
- agent 访问页面时**发现**这些工具,直接调用——不再靠 DOM 抓取/截图/猜按钮
- 工具在浏览器内执行、可见地更新 UI,用户保持在场与信任

官方 demo 实测:agent 调 WebMCP 工具完成任务约 **5 秒**,传统浏览器 agent(DOM 抓取 + 截图 + 模拟点击)同任务 **30-60 秒且不稳定**。

**WebMCP 明确声明了 Non-Goals(不做的事)**:
- ❌ headless 场景(主要面向本地浏览器 + 人在环路)
- ❌ 完全自主的工作流(要求人工监督)
- ❌ 替换后端 MCP / 替换人类界面
- ❌ 要求网站必须适配才可用(是渐进增强,不是门槛)

---

## 二、两条路径:本质对比

业界解决"agent 操作网页"有两条相反的路径,本项目与 WebMCP 恰好各占一端:

```
路径 A(WebMCP):网站主动为 agent 留门     —— 网站声明能力,agent 直接调用
路径 B(本项目):构建智能体直接理解的原生网页世界 —— agent 侧主动理解任意网页
```

| 维度 | WebMCP(路径 A) | 本项目(路径 B) |
|---|---|---|
| 本质 | Web 标准提案(W3C 孵化中) | 运行时 MCP 服务器(注入内核 + 结构化空间) |
| 谁适配谁 | 网站适配 agent(开发者注册工具) | agent 适配网站(零依赖,注入即用) |
| 前提 | 网站必须主动实现 WebMCP | 任意网站,无需任何配合 |
| 准确度 | 网站声明语义 + JSON Schema,无歧义 | 结构化空间"读"出来(编号/名字/坐标),靠增量+验证逼近准 |
| 速度 | 直接调函数(demo 实测 5 秒) | 读页面+模拟操作+状态卡(30-60 秒量级) |
| 适用形态 | 本地浏览器 + 人机协同 | headless + 并行多开 + 可全自主(配人工介入点) |
| 认证 | 继承浏览器 SSO/session/RBAC | profile 持久化登录态 |
| 生态 | 需要浏览器 + 网站开发者共同推进(漫长) | 现在就能用,11 站点实测 |
| 当前状态 | 标准在演进(API 名仍变动中),2026 年中后 Chrome/Edge 才支持 | 已可用 |

**最关键的一点**:WebMCP 的 **Non-Goals 恰好是本项目的 Goals**——

| WebMCP 明确不做 | 本项目恰好做 |
|---|---|
| headless 场景 | headless + 并行多开 |
| 完全自主(无人监督) | 全自主 + 人工介入点 |
| 替换后端 MCP | 本身就是 MCP 服务器 |
| 要求网站适配 | 零适配任意网站 |

**结论:二者不是竞争,是互补的两端。** WebMCP 解决"网站配合时怎么快准",本项目解决"网站不配合时怎么也能做、以及如何验证做对了"。

---

## 三、对项目价值方向的校准

### 1. 本项目有一个"从现在到未来都成立"的位置

WebMCP 落地需要整个 web 生态配合(浏览器 + 网站开发者),是漫长的"鸡生蛋"过程。**在 WebMCP 普及之前(以及覆盖不到的地方),agent 操作网页只能靠"读取+模拟"——这正是本项目的领域。**

本项目不是"过渡的临时方案",而是"**WebMCP 覆盖不到领域的长期方案**"。

### 2. 最理想未来形态 = 融合(本项目的演进方向)

```
页面 → 有 WebMCP 工具? → 直接调工具(快准省)     ← WebMCP 层
                    ↓ 没有/失败
       原生网页世界读取 + 行动层模拟(兜底)        ← 本项目层
       操作后用原生网页世界验证结果(双层校验)     ← 本项目独有
```

即:**原生网页世界作为"读 + 验证"层,WebMCP 作为"做"层**。WebMCP 工具执行完,用原生网页世界/状态卡验证"是否真的生效"——这是纯 WebMCP 没有的能力,是本项目的独特接口点。

### 3. 现在就应该留的"未来兼容"接口

- `findEntities/getEntity/status` 已是天然的结构化接口,未来可**同时感知页面注册的 WebMCP 工具**(监听 `document.modelContext` 或类似 API):有工具优先走工具,没有则走模拟
- 项目由此从"纯模拟工具"升级为"**浏览器 agent 的执行调度层**"——先试最快路径,再降级,永远兜底

### 4. 差异化卖点(对外表述)

> "**WebMCP 普及之前和之外,agent 操作网页的可靠底座**:网站没为 agent 留门时(99.9% 的网站),原生网页世界让 agent 自己读懂并操作;将来网站留门了(WebMCP),原生网页世界做验证兜底。"

---

## 四、路线图建议

| 优先级 | 事项 | 说明 |
|---|---|---|
| 现在 | 术语统一:全库"世界模型"→"原生网页世界" | 本文件已声明,配套文档同步 |
| 近期 | 点击验证闭环 | 操作后对比变更流,回报"是否生效" |
| 中期 | 语义摘要 + 重要性加权 | 让 agent 每轮少读一个量级(快) |
| 远期 | WebMCP 兼容感知 + 验证兜底 | 监听页面工具,先工具后模拟,永远兜底 |
| 备用 | 真实浏览器 CDP 桥接 | **已降级:机制保留为实验特性(仅独立 profile),连日常浏览器场景暂缓,安全设计(端口鉴权/域白名单/审计)独立立项评估** |

---

## 附:参考资料

- WebMCP 提案:https://github.com/webmachinelearning/webmcp
- Chrome 文档(WebMCP | AI on Chrome):https://developer.chrome.com/docs/ai/webmcp
- 解读文章(WebMCP: the missing bridge between AI agents and the web):https://www.scalekit.com/blog/webmcp-the-missing-bridge-between-ai-agents-and-the-web
