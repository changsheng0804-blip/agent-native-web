# 实时闭环反馈 · 实战验证方案

> 版本:2026-08-31
> 主题:对"实时闭环反馈"三件套(`world_changes` 变更可读化 / `world_click` 生效报告 / `world_wait` 事件驱动)做多站点多场景实战验证,**目标是找出失败模式**,不是证明成功。
> 依据:GF 实测已证明"全页 diff 是重渲染噪声、元素 ID 会失效"——闭门优化会优化在错误方向上,必须先实战。

---

## 一、验证目标

回答三个问题:

1. **`effect` 的判定准不准**——点击后 `verdict/confidence` 与"真实是否生效"是否一致?尤其四种易翻车场景(远距弹窗/表单导航/标签切换/持续变更)。
2. **`digest`/`importance` 是不是好摘要**——噪声是否被降权、真信号是否被标高、摘要是否少而准。
3. **`waitFor` 事件驱动在真实页面上可不可靠**——持续变更页会不会饿死/误触发,命中与超时是否符合预期。

**输出**:一张「场景 × 判定一致性」矩阵 + 失败模式清单 + 由此决定的修正路线。

---

## 二、验证对象与参数假设(待实测的"假设")

| 组件 | 现状 | 待验证的核心假设 |
|---|---|---|
| `world_click` `effect` | 点击前冻结目标 bounds ±200px → 点击后轮询区域 diff(有变化即停,≤2.5s)→ verdict | **H1** 目标区域 ±200px 能覆盖操作结果;**H2** 区域内新增关键语义构件=生效的充分证据;**H3** URL 变化=导航类生效的可靠信号 |
| `world_changes` digest/importance | 事件带 semantic;server 按"事件类型×语义角色"分级 + digest | **H4** 全页 digest 在持续变更页不会信息爆炸;**H5** importance 分级与"对 agent 的价值"一致(真弹窗 high,广告刷新 low) |
| `world_wait` waitFor | 内核 waiter,MutationObserver flush 后检查 | **H6** 持续变更页(observer 一直被重置)下 waitFor 仍能命中;**H7** 命中/超时耗时符合预期(不饿死、不提前) |

---

## 三、失败模式假设(要主动去找的四种坑)

| 编号 | 场景 | 预期风险 | 影响 |
|---|---|---|---|
| F1 | **远距弹窗**:触发点在角落,弹窗/面板出现在屏幕中央或远处 | ±200px 区域**漏掉**结果 → `no-change` 误判(FN) | agent 以为没点中 |
| F2 | **表单提交/导航**:点击后无新弹窗,只有 URL/页面变化 | 区域 diff 无变化 → 依赖 URL 分支;URL 未变但页面内容换了 → 误判 `no-change` | 提交类任务假失败 |
| F3 | **标签页/下拉切换**:SPA 整块重渲染,无 dialog | 区域大量增删 → 误判 `effected`(FP),而真信号可能只是 aria-selected | agent 以为生效了(最危险:FP) |
| F4 | **无限滚动/持续变更**:observer 一直被重置 | `waitFor` 饿死或提前命中;digest 噪声爆炸 | 等待不可靠、摘要无用 |

---

## 四、Truth Oracle(真值判定——验证的锚点)

对每个场景,定义**独立于 effect 的"真实是否生效"判定**(用 `world_eval` 直接查 DOM/URL,不走原生网页世界判定逻辑):

| 场景 | Truth 判定(DOM/URL 直查) |
|---|---|
| 弹窗/菜单/面板 | `document.querySelector('[role=dialog], [role=menu], [aria-modal=true]')` 存在且可见(rect>0、非 hidden) |
| 表单提交/导航 | `location.href` 是否变化(与点击前对比) |
| 标签切换 | 目标 tab 的 `aria-selected="true"` 或内容容器 `hidden` 属性翻转 |
| 折叠/展开 | 目标元素的 `aria-expanded` 或 class 变化 |
| 负例(无副作用) | 以上全部无变化 |

**Truth 是"上帝视角"**:DOM 直接反映页面真实状态,与 effect 的空间区域推断解耦。

---

## 五、判定一致性矩阵(核心输出)

每个场景跑完后,把 `effect.verdict` 与 Truth 对比,归类:

| 分类 | 含义 | 严重度 |
|---|---|---|
| TP | Truth 生效 ∧ verdict=effected | ✅ |
| TN | Truth 未生效 ∧ verdict=no-change | ✅ |
| **FP** | Truth 未生效 ∧ verdict=effected | 🔴 最危险(agent 误以为成功) |
| **FN** | Truth 生效 ∧ verdict=no-change | 🟠 agent 误以为失败 |
| AM | Truth 生效 ∧ verdict=changed/medium | 🟡 低置信,可接受但应改进 |

**通过线**:TP+TN ≥ 80% 且 FP=0(FP 一票否决,说明判定过激,必须收紧)。

digest 侧另记:**high 事件里噪声占比**(真信号/总 high 数),理想 >70%。

---

## 六、场景矩阵与站点选择

> 站点优先复用已验证可达的(README 实战矩阵),避免反爬/网络干扰污染验证;本地夹具用于**精确制造**四种失败模式(可控、可复现)。

| 场景 | 站点/夹具 | 操作 | 期望 Truth | 专测假设 |
|---|---|---|---|---|
| 就近弹窗(基线) | GF 乘客按钮 | 点击乘客 | dialog 出现 | H1~H3 基准 |
| F1 远距弹窗 | 本地夹具 `far_modal.html` | 点角落按钮 → 居中弹窗 | dialog 出现(远离按钮) | **H1** 漏判 |
| F2 表单/导航 | 百度搜索框 | fill+Enter 提交 | URL 变化 | **H3** |
| F3 标签切换 | 本地夹具 `tabs.html` | 点第二个 tab | aria-selected 翻转 | **H2/F3** FP |
| F4 持续变更 | Booking(已知重型) | 打开 + 滚动/等待 | waitFor 命中 + digest 不爆 | **H4/H6/H7** |
| 负例 | dyn.html 标题 | 点无副作用标题 | 无变化 | TN 不误报 |
| 跨站抽查 | Wikipedia / GitHub | 各 1~2 个常规点击 | 视页面 | 通用性 |

> 真实站点布局随时变,若目标元素找不到则记录"SKIP",不硬凑。

---

## 七、运行方式与脚本

实现一个**配置驱动验证脚本** `validate_closed_loop.py`(agent-world-mcp/),每个场景一个配置项:

```python
CASES = [
  {
    "name": "gf-passenger",
    "url": "https://www.google.com/travel/flights",
    "setup": ...,            # 可选:world_open 后准备
    "find": {"name": "passenger"},   # 目标构件过滤
    "action": "click",       # click 或 fill+key
    "truth_js": "...",       # Truth 判定表达式(返回 bool)
    "expect": "effected",    # 期望 verdict
  },
  ...
]
```

输出:
1. 控制台:每场景一行 `[TP/FP/FN/AM] name verdict=.. truth=.. digest=..`
2. 报告文件 `validate_report.md`:矩阵表 + 失败模式清单 + 修正建议
3. 失败场景自动重跑一次确认可复现

**流程**:本地夹具全过(验证脚本与 oracle 本身正确)→ 再跑真实站点(避免网络因素干扰夹具归因)。

---

## 八、路线决策规则(验证结果 → 下一步)

| 结果 | 决策 |
|---|---|
| 全场景 TP/TN 达标,FP=0 | 假设 H1~H7 成立 → 横向复制:fill/press 带 effect、证据窗"等 changelog 安静"细化 |
| 只有 F1 类 FN(区域漏判) | 修正区域策略(如:目标中心 + 全局 dialog 兜底扫描)→ 重验 |
| 出现 FP | **先收紧判定**(高置信门槛、要求 dialog/URL 之一)→ 重验;不横向复制 |
| F4 waitFor 饿死/误触发 | 修内核 waiter(如 observer 饿死保护复用 maxWait 机制)→ 重验 |

**原则**:出现 FP 时暂停所有横向扩展,先解决判定过激——FP 会让 agent 把失败当成功,是闭环可信度的头号杀手。

---

## 九、范围与边界

- 只验证**读/等待**闭环(digest/effect/waitFor),不改 action 层点击本身
- 反爬拦截页(Reddit/eBay)不纳入本次(已确认 anomaly 不误报,与闭环无关)
- 每站点单次会话,避免长时间占用;失败场景重跑一次确认可复现,不穷举
- 不验证视觉通道(screenshot 与结构层一致性属另议)

---

## 附:实施清单

1. 本地夹具 `far_modal.html`、`tabs.html`(test_fixtures/,与 dyn.html 同风格) ✅
2. `validate_closed_loop.py`(配置驱动 + truth oracle + 报告) ✅
3. 跑:本地 4 场景 → 真实 4 站 → 生成 `validate_report.md` ✅
4. 按第八节决策规则定路线,回填本方案/交接文档 ✅

---

## 附:验证执行记录(2026-08-31)

### 结果:通过线达标(83% ≥ 80%,FP=0)

| 场景 | 分类 | 结果 |
|---|---|---|
| gf-passenger(就近弹窗) | TP | effected/high |
| far-modal(远距弹窗) | TP | effected/high |
| tabs(标签切换无弹窗) | TP | effected/high |
| negative-heading(负例) | TN | no-change |
| baidu-submit | AM | URL 变但被百度反爬验证码拦截(环境问题,非代码 bug) |
| wiki-link | TP | effected/high(URL 导航分支) |
| waitFor 持续变更专项 | PASS | 0.03s 事件命中 |

### 实战发现的两个真实问题(均已修复)

1. **F1a 内核:observer 漏 `open` 属性**。HTML 原生 dialog/details 用 `open` 属性切换显隐,原 attributeFilter 不含 `open` → 弹窗变可见但世界不注册(`world_entities role=dialog` 查不到),且 effect 的 dialog 兜底扫描世界也找不到。修复:`content/observer.js` attributeFilter 补 `open`/`aria-expanded`/`aria-selected`(状态与显隐类属性),重建 all-in-one.js。**验证:far-modal 从 FN → TP**。
2. **F1b server:±200px 区域漏远距弹窗**。修复:effect 判定加**全页可见 dialog/menu 兜底扫描**(点击前没有、点击后有 → effected/high,"页面出现新的弹窗/菜单(可能远离目标)")。**验证:far-modal 判 TP**。
3. **F3 状态切换类交互(无新构件)**:tab 切换只变 `aria-selected`,区域无新增关键构件 → 原判 changed/medium。修复:快照/轮询采集**目标自身状态**(aria-selected/aria-expanded/checked/class),翻转 → effected/high。**验证:tabs 从 AM → TP**。

### 结论与路线

- **假设 H1~H7 成立**(±200px 区域 + 全页 dialog 兜底 + 目标状态翻转 + URL 变化 + 事件驱动 waitFor)
- 按决策规则:**通过线达标 → 可横向复制:fill/press 带 effect、证据窗"等 changelog 安静"细化**
- 遗留:百度反爬验证码属环境问题(非闭环缺陷);fill 无 effect 是已知待办(横向复制时补)

### 第二轮:真网站扩展 + 证据窗升级(2026-08-31)

**真网站扩展**:新增 Wikipedia 搜索联想(填表)、GitHub 仓库标签(导航)、Stack Overflow 搜索。结果 TP=8 TN=2,准确率 100%,无假成功/假失败。SO 被反爬挡(SKIP,环境问题)。

**证据窗基线测量(升级前)**:普通操作 0.2s 看到变化却等满 0.6s(多等 0.4s 稳定);持续变化页(维基联想)等满 1.6s——"看到变化后固定等 0.4s 稳定"对已见决定性证据的 case 是纯浪费。

**升级方案评估**:
| 方案 | 收益 | 成本 | 结论 |
|---|---|---|---|
| 聪明早停(当前,已实现) | 普通 0.6→0.2s,持续变化 1.6→0.21s | 每轮轮询多算一次 effect(轻量) | ✅ 采用 |
| 纯事件驱动(内核 waitFor 驱动证据窗) | 再省 ~0.2s 轮询间隔 | 内核复杂度上升、异常分支多 | ❌ 边际收益<成本,不采用 |

**升级后实测**:正确性不变(TP=8 TN=2,100%),普通操作 ~0.2s、持续变化页 ~0.21s。effect 新增 `evidence:{polls,total_ms,first_change_ms,stop}` 计时字段,便于后续持续观测。

### 第三轮:digest/importance 价值评估 + 重要性降权(2026-08-31)

**评估方法**:`validate_closed_loop.py --digest`——多真站执行动作后读 `world_changes` 的 digest,量化三件事:压缩比(事件数/摘要字符)、强信号数(highlights 中 dialog/menu/option/combobox/input)、外壳噪声数(highlights 中 button/link/navigation 等)。

**实测发现(修复前)**:全局 8799 事件→1213 字符摘要,压缩比 7.3(少读有价值 ✅);但 highlights 中**噪声 29 vs 强信号 5**——`_IMPORTANT_ROLES` 把 button/link/navigation 标 high,重型 SPA(Google Flights/Amazon/BBC/维基)整体重渲染时外壳"假新增"刷屏,把真弹窗挤出前 6 名(🔴 重要性加权标错重点)。

**修复**:digest 口径改用窄集合 `_DIGEST_HIGH_ROLES`(dialog/alertdialog/menu/option/listbox/combobox/input/select/searchbox/textbox),外壳角色(button/link/navigation/tab/form 等)降 medium。**关键设计:与 effect 判定分离**——effect 仍用宽口径 `_IMPORTANT_ROLES`(按钮/链接出现也是操作结果的间接证据,避免"展开子菜单"类操作被降权回归)。

**修复后实测**:
- 强信号 14 vs 噪声 0 ✅(GF digest 第一行从 `skip-to-main-content` → `弹窗 dialog.number-of-passengers`)
- 摘要总字符 1213→623,压缩比 7.3→14.1 事件/字
- effect 判定不受影响(完整体检 TP=9 TN=3,100%,含新增 Amazon/BBC/GitHub 场景)

**结论:语义摘要+重要性加权的真正价值成立,但前提是 importance 口径必须区分"操作结果信号"与"重渲染假新增"**——否则会帮倒忙(噪声刷屏)。窄口径 + 与 effect 分离是当前最优解。

### 第四轮:digest 改结构化语义摘要(counts+key,CAD 图纸 004# 圆孔原则)(2026-08-31)

**设计原则(用户拍板)**:摘要不写人话句子(如"乘客面板弹出"是 server 替 agent 下结论,越俎代庖);用**强 ID 引用**(`el_595`)描述——如 CAD 图纸上的"004# 圆孔",编号即一切属性(位置/属性/邻居/区域)的入口,agent 用 `world_entity(id)` 查详图,无歧义、不猜。

**改动**:`_change_digest` 返回从 `{summary,counts,highlights}` 改为 `{counts,key}`:
```json
{"counts": {"add":108,"remove":851,"update":949},
 "key": [{"type":"add","id":"el_595","semantic":"dialog","name":"dialog.number-of-passengers"}]}
```
key 仅含高重要 add/remove 的强 ID 引用(上限 10 条)。

**验证**:test_change_digest 断言 `summary` 不存在、`key` 存在、且 key 的 dialog 强 ID 能 `world_entity` 查详图(管线闭环)。`--digest` 评估新增 `管线=True/False` 列:有 key 的场景全部 True(GF/百度/维基搜索)。完整体检 TP=9 TN=3 100% 无回归。

### 第五轮:稳定指纹(第二 ID)真站验证 + 语义 id 优先(2026-08-31)

**背景**:指纹为同站多次进出提供"认路记忆"(B 方案,仅同一次对话内,不落盘)。先本地验证(tabs 页 8 元素 8 唯一指纹、两次进站一致、按指纹一步定位),再上真站。

**真站两轮对比**(`test_fingerprint_real.py`,六站:维基/百度/亚马逊/BBC/GitHub/GF,每站两次 world_open + 指纹采样查询):

| 站点 | 命中率(前→后) | 核心命中(前→后) | 撞车(前→后) |
|---|---|---|---|
| 维基 | 90→92% | 92→92% | 8→6 |
| 亚马逊 | 81→86% | 81→86% | 12→9 |
| GitHub | 81→88% | 82→85% | 14→10 |
| 百度 | 89→85% | 94→92% | 9→11 |
| GF | 71→72% | 89→87% | 23→22 |
| BBC | 65→62% | 58→56% | 28→30 |
| 平均 | 80→81% | 83% | 94→88 |

**发现与修复**:随机 id(GF `vfppkd`、React `__react-xxx`)每次渲染会变,污染指纹跨会话稳定性 → 新增 `isSemanticId`(含 `-`/`_` 分隔符或单字含元音=语义 id 才进指纹;纯数字/全辅音/`__` 开头=随机丢弃)+ 祖先路径 3→5 层。改进后亚马逊/GitHub/维基明显提升,核心控件命中率 83%(可用)。

**结论(分层原则)**:操作目标(有语义命名的登录/搜索/导航/表单)用指纹认(85~92%,这就是认路记忆的用武之地);无命名重复内容(新闻卡片,如 BBC)是信息论边界——没有语义信息就无法稳定识别,但**其内容/文本本身就是天然 ID**,需要时用 `world_entities(text=...)` 关联即可。**停止在指纹上继续优化(收益递减+风险),接受现状。**

### 第六轮:world_map 页面结构导览(地图)(2026-08-31)

**背景**:中期目标"让 agent 每轮少读一个量级"。现有 `world_entities`(200 条平铺清单)/`world_layers`(统计报表)都不是"地图"——缺一张"一眼懂布局"的整体空间视图。类比:图纸零件表 ≠ 地图;`world_map` = 按语义容器分区 + 各区可交互入口(带强 ID/指纹)。

**设计(最小版)**:找语义地标容器(navigation/banner/main/contentinfo/complementary/form/dialog/tablist 等,面积>视口 2%)当街区锚点,构件按"中心点归入最小包含锚点"聚合,每区给范围/构件数/可交互入口。`world_map(max_entries=6)`。

**多真站体检**(`test_map_real.py`,六站):维基 1% 散件、谷歌机票 4%、BBC 7%、亚马逊 19%、GitHub 72%、百度 99%(0 语义容器)。入口钻取全部 4/4。**发现并修复重叠锚点噪音**:多个 header/导航容器叠同一位置产生空区域(18 个)→ 锚点去重叠(交集占较大面积≥85% 视为重合,只保留面积最大;注意必须用大面积判据,曾用交集/小面积把 GitHub 误并成 3 区已修正)+ 过滤空壳区域。修复后空区域 18→0。

**地图设计原则(大区域重新定义,2026-08-31)**:
- **地图的价值是方向感,不是穷举清单**。`main` 吞 1940 构件不是 bug,是页面语义结构的如实反映(GitHub 的 `<main>` 确实包住几乎整页主体)。
- **大区域是有效信号**("内容都在这里"),不是缺陷——agent 读到"main 区 1940 构件"应理解为"干货在主体区,去这里找",而非"逐个看这 1940 个"。
- **细看靠查询工具,不靠地图**:`world_entities`(role/text/name/fingerprint 过滤)+ `world_entity`(详图)是找具体目标的正确通道;硬切 main 反而会产生"伪区域"噪音,且对 agent 无实际帮助。
- **百度类纯 div 页(0 语义容器)不强求地图**:散件里能找到那个输入框即可(操作目标用指纹/文本找的分层逻辑)。
