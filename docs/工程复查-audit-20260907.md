# 工程完整性与管理一致性复查

复查日期：2026-09-07（美洲太平洋时间；部分远程记录为 UTC 2026-09-08）。

## 范围与起点

【已确认】远程为 `changsheng0804-blip/agent-native-web`，默认分支 `master`；本地活动仓库初始工作树干净，主线与远程同为 `3252d866c83ecb5501229757a9bf545fce975e36`。依据：本地 Git 状态、获取远程后的分支比较、GitHub 仓库接口。

【已确认】上次整理已完成文档归档、运行产物隔离、工具模块拆分、提交追溯钩子和 32 项离线测试登记。初始 GitHub 最近一次运行成功，但只执行两项冒烟测试（仅检查核心流程是否能启动），不等于完整离线验收。

## 复查发现与处理

| 优先级 | 已确认的发现 | 处理 |
| --- | --- | --- |
| 高 | 主分支无保护，文档要求完整门禁而远程只跑两项 | 完整离线双版本验收与统一汇总结果；主分支规则改为合并条件 |
| 高 | 安装入口各自列依赖，部分未限制 MCP 大版本 | 单一声明与全依赖版本锁定；检查实际安装与锁定结果 |
| 中 | 旧工作区指向改名前地址，旧分支有 5 个独有提交 | 明确活动入口、历史推送隔离；保留提交的归档标签 |
| 中 | 架构指南写 22 个工具，并推荐旧操作链 | 依据工具定义核为 40 个（6 个标准入口、34 个调试兼容入口），统一主流程 |
| 中 | 范围别名可能带入真站，混合拼错参数未拒绝 | 明确离线选择边界；错误输入立即终止，新增 8 项入口回归断言 |
| 中 | 合并后自动删分支关闭，无反馈模板和待决事项入口 | 启用自动清理，建立中文模板和长期治理台账 |
| 中 | 结果报告日期写死，缺少可持续一致性核查 | 记录实际时间与耗时，新增治理检查并纳入远程门禁 |
| 中 | 扩展按钮声明 `about:blank`，旧面板被误认为活动界面 | 改为本地中文说明页，标注旧原型范围；当前覆盖层中文显示 |
| 待决 | 未声明项目许可证，无正式发行记录 | 登记 GOV-01；未替所有者新增许可、创建发行版 |

## 验证记录

【已确认】改动前完整离线基线：32/32 通过，Windows / Python 3.12.10，3 路并行，实际耗时 173.2 秒；原环境 MCP 1.29.1、Playwright 1.60.0。报告见[离线基线](archive/离线基线-quality-20260907.md)。

【已确认】治理后本地完整离线验收：33/33 通过，Windows / Python 3.12.13，3 路并行，168.7 秒；锁定环境 MCP 1.30.0、Playwright 1.62.0。依赖兼容性、工程一致性、正确性检查、JavaScript 语法与中文弹窗/内核启动检查均通过。见[离线复验](archive/离线复验-quality-20260907.md)。

【已确认】GitHub 配置回读：主线强制合并申请与“工程验收”（限定 GitHub Actions 应用，编号 15368）、严格更新主线、解决讨论；管理员同样受限，禁止强推与删除；只允许压缩合并，合并后自动删分支。凭据扫描、推送拦截、依赖漏洞提醒、安全修复和私密漏洞报告均已启用。10 个原有标签已改为中文，仓库没有待处理议题。

【已确认】旧分支归档标签 `archive/legacy-action-20260907` 解析到 `44a44132ea33859e5f3d05115bf502efc0e3b5eb`，与删除前分支完全一致，5 个独有提交仍可追溯。外层 113 个历史文件保留并生成 SHA-256（文件内容校验值）清单；外层仅更新本地导航、远程规范地址和推送隔离配置。

【不确定】远程新流水线需在本次合并申请上实际执行；本地通过不替代 GitHub 上的最终结果。

## 结论边界

【推断】项目已具备可验证的实验工程基础；此前主要缺口是规则没有完全落实到自动执行，而非完全缺少测试或模块结构。

【不确定】本次治理不是穷尽全部代码路径的安全审计，不证明所有真实站点、登录场景或浏览器商店安装方式均可用。两项人工实验和旧原型不计入产品验收范围。

【建议】按[治理台账](项目治理-governance.md)推进许可决策与发布验收；后续以实际通过的提交编号和验收运行记录作为工程状态依据。
