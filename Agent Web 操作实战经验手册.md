# Agent Web 操作实战经验手册

> 版本:v1.0 · 2026-08-31
> 来源:agent-world 技能包在真实站点(Google Flights / 闲鱼 / Google Labs)实战中踩坑与验证的经验沉淀
> 配套:《Agent Web 操作技能包-整体设计》《世界状态卡-实施计划》

---

## 一、环境可信度(最重要的经验)

**浏览器环境决定页面"给不给内容"。** 世界模型忠实反映页面;页面不诚实(反爬/简化页)时,世界模型也"不诚实"——这是环境问题,不是世界模型 bug。

### 环境可信度分层(实测)

| 环境 | 闲鱼搜索页实测 | 可信度 |
|---|---|---|
| headless(headless-shell) | 161 元素简化页,无商品,提示"非法访问" | 低(多数反爬站点拦截) |
| headful(Chrome for Testing)匿名 | 638 元素,正常渲染 | 中(GF 拒绝登录,闲鱼可用) |
| headful + 登录 profile | 1201 元素但**增删循环页**(add 33/remove 34 动态平衡),商品被藏 | 中低(登录态触发更强保护) |
| 用户日常 Chrome(未来扩展形态) | 未测(预期最高) | 高 |

### 反爬的三种表现(都已实测)

1. **简化页**:headless 返回骨架(闲鱼 161 vs 638)
2. **拒绝服务**:Google Flow 拒绝 Chrome for Testing 登录("当前环境不安全")
3. **增删循环页**:登录态下渲染动态不稳定页面,世界模型元素数维持低位

### 应对策略(按优先级)

1. **anomaly 信号**:状态卡 `page.state=anomaly`(世界模型 vs 可见 DOM 缩水 <35%)——先识别,再行动
2. **默认 headful**:已知复杂/敏感站点直接用 headful,不用 headless 试探
3. **视觉兜底**:anomaly 时截图 + 视觉模型确认页面真实状态
4. **人工介入**:遇到登录/验证码,弹窗交给人
5. **换站点/接受局限**:对抗反爬是猫鼠游戏,不值得深投入(指纹伪装等)

## 二、分层加载与读取时机

**世界模型是持续增长的,读取时机决定看到什么。** 页面渐进渲染(骨架 → 首屏 → 懒加载 → iframe 延迟加载)。

- **"世界模型就绪 ≠ 页面就绪"**:agentWorld 挂载只是 bootstrap 完成(初始扫描),增量更新还在进行
- 固定等待秒数不可靠(同一页面不同会话 6s 时世界 27-43 个,12s 后 220+ 个)
- **正解:等待状态卡 `page.state=stable`**(内核稳定性判断:元素数连续两次一致 + readyState),`world_open` 的 `stabilize_ms` 参数实现
- 懒加载/无限滚动页面:世界模型持续增长,查询时注意规模变化(changesSeq 反映)

## 三、iframe:内容分层

- **关键 UI 常在 iframe**:闲鱼登录框(289 元素)、跨域存储帧;主文档 scanner 看不到 iframe 内容
- **每 frame 一个独立世界**(add_init_script 覆盖所有 frame),坐标是 frame 内相对坐标
- 状态卡 `frames: [{url, elements, visible, ready}]` 逐层报告;agent 先看 frames 再决定操作哪个层
- 注意:iframe 内世界编号独立,可能与主文档冲突——跨层操作需 frame 定位(规划中)

## 四、表单与受控组件

- **input.value 快照会过期**:React 受控组件 fill 后可能重置 DOM value(Google Flights 实测:fill 后 300ms 有值,600ms 被重置)
- `getEntity` 实时读 value 解决查询时刻的准确性;表单状态卡对普通站点可靠,重受控组件站点不稳定 → 视觉兜底
- **覆盖层副本**:SPA 点击输入框后渲染"激活态副本"盖在原输入框上,fill 到下层无效 → 自动切换覆盖层(已实现)
- 弹窗遮挡:先开的面板会挡住后续操作目标,操作顺序要考虑遮挡(点面板外/Escape 关闭)

## 五、会话持久化

- **扫码/快速登录通常只种 session cookie**:浏览器关闭即失效,Chromium profile 目录也存不住
- **正解:storage state 导出/恢复**(`world_close` 导出含 session cookie,重开同 profile 自动恢复)——已实现
- 持久 cookie 与 session cookie 并存:Google(长期 cookie)关闭重开仍登录;闲鱼(多为 session cookie)必须靠 storage state

## 六、世界模型的边界(诚实清单)

| 世界模型能 | 世界模型不能 |
|---|---|
| DOM 投影 + 空间 + 语义 + 变更流 + 表单值 | 像素级内容(图片文字/图表/验证码)→ 视觉 |
| iframe 分层感知(状态卡) | 反爬视图识别(已用 anomaly 部分解决) |
| 登录态(双信号) | 需要人工的环节(验证码/真人)→ 弹窗给人 |
| 状态卡主动暴露 | 页面拒绝渲染时无能为力(anomaly 提示换路) |

**分工原则**:世界模型负责"看"(结构化、低成本);Playwright 负责"做"(自动等待/手势);视觉负责"疑难杂症";人负责"身份与最终确认"。

## 七、测试经验

- **真实站点测试不可替代**:Google Flights 首测暴露 SVG 崩溃;闲鱼暴露 iframe/反爬/会话问题
- **本地可控页面做机制验证**(dyn.html 动态页),真实站点做场景验证——两者分开,定位更快
- 每次"失败"先区分:机制 bug / 站点特性 / 反爬环境——三个方向排查路径完全不同

---
*本手册随实战持续更新。*