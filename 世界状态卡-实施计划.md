# 世界状态卡(World Status Card)实施计划

> 版本:v1.0(已实施)· 2026-08-31
> 状态:**✅ 已完成**(T1-T5 全部落地)
> 关联:《Agent Web 操作技能包-整体设计》(技能包 v1.0)

---

## 实施记录

| 任务 | 状态 | 说明 |
|---|---|---|
| T1 内核状态维护 | ✅ | `runtime.js` refreshStatus(dialogs DOM 直查+可见性过滤 / forms / page);`query.js` getStatus() |
| T2 server 聚合注入 | ✅ | `_auth_status` 双信号(cookie+DOM);`_status` 聚合+changed 高亮;`_inject_status` 注入所有工具返回;操作后 `_refresh_core_status` 主动刷新 |
| T3 测试 | ✅ | 4 场景全过:状态卡注入 / forms(本地页 value)/ dialogs(GF 乘客面板)/ auth(闲鱼 cookie 登录) |
| T4 文档 | ✅ | 本文件 + README + 技能包 |
| T5 git | ✅ | 提交 `世界状态卡` |

**实测样例**(Google Flights + 闲鱼):
```
auth:    {loggedIn: true, via: "cookie:.goofish.com"}   ← 闲鱼登录态
dialogs: [{name: "number of passengers"}]               ← 乘客面板
forms:   [{id: "el_5", value: "hello world"}]           ← 本地页 fill
changed: {dialogs: true, forms: true}                   ← 变化高亮
```

**已知边界**:
- Google Flights 受控组件会重置 input.value → forms 在该类站点不稳定,普通站点可靠;视觉兜底
- 登录推断以 cookie 信号为主,DOM 特征辅助;个别网站可能误判
- 状态卡 ~224 字符/次,可接受

---

## 一、背景:要解决的问题

当前 agent-world 是**被动查询型**:agent 每步调 `world_entities` / `world_entity` / 截图,登录没登录、弹窗开没开——这些状态**藏在 DOM 细节里,靠猜**。截图方案更差:静态图、反馈慢、不准。

**目标:原生网页世界像飞机仪表盘**——时刻显式、结构化、实时地暴露"当前状态"(登录态/弹窗/页面/表单),agent 每次操作后返回里直接带最新状态,不用额外猜。

## 二、设计:世界状态卡

### 2.1 状态卡结构(JSON,目标 <200 字符)

```json
{
  "auth":    { "loggedIn": true, "account": "qyf20220527888", "via": "cookie:passport.goofish.com" },
  "dialogs": [{ "id": "el_560", "name": "dialog.login-quick", "open": true }],
  "page":    { "state": "stable", "scrollY": 0, "totalHeight": 3032 },
  "forms":   [{ "id": "el_128", "name": "combobox.where-from", "value": "Tokyo" }],
  "world":   { "elements": 461, "changesSeq": 152 }
}
```

### 2.2 各字段:推断来源与可靠性

| 字段 | 内容 | 推断来源 | 可靠性 | 实现层 |
|---|---|---|---|---|
| `auth` | 登录态+账号 | **双信号交叉**:① server 层读 cookie(登录域如 `passport.goofish.com`,HttpOnly 也能读,可靠)② DOM 特征(用户名出现/登录按钮消失) | 高 | MCP server |
| `dialogs` | 打开的弹窗/对话框 | `role=dialog` / `aria-modal` / 常见弹窗类名,MutationObserver 增量维护 | 高(ARIA 标准) | 内核 |
| `page` | 加载/稳定、滚动位置 | readyState + 滚动/尺寸 + 近期 DOM 变化频率 | 中高 | 内核 |
| `forms` | 有值/聚焦的表单 | 已有 value 实时机制,聚合非空输入框 | 高 | 内核 |
| `world` | 世界规模、变更流游标 | 已有 meta + changelog | 高 | 内核(已有) |

### 2.3 注入时机(核心设计)

**每次工具返回自动附带状态卡**(`world_open` / `world_entities` / `world_click` / `world_fill` / ... 所有工具)。

效果:我点完"登录按钮",返回里直接看到 `auth: {loggedIn: false → true}` 的实时变化——**每一步操作都有仪表盘,零额外查询**。变化高亮:`changed` 字段标注本轮刚变化的状态(如登录态刚翻转)。

## 三、实现架构(分两层)

```
┌─────────────────────────────────────────────┐
│ MCP server(server.py)                        │
│  · auth 检测:context.cookies() + DOM 特征    │
│  · 状态卡组装:auth + 内核状态 → 注入工具返回  │
├─────────────────────────────────────────────┤
│ 内核(蓝图版 engine/content)                  │
│  · dialogs/page/forms 状态维护(增量)         │
│  · query.getStatus() 暴露                    │
└─────────────────────────────────────────────┘
```

## 四、验证方案

| # | 场景 | 预期 |
|---|---|---|
| 1 | 闲鱼未登录打开 → 登录 → 重开 | `auth.loggedIn` false → true,重开后仍 true |
| 2 | 点击弹出弹窗(Google Flights 乘客面板) | `dialogs` 出现该弹窗构件 |
| 3 | fill 输入框 | `forms` 出现 value |
| 4 | 状态卡大小 | 每次返回 <200 字符(不浪费 token) |
| 5 | 操作后返回附带状态卡 | click/fill 返回中直接见状态变化 |

## 五、风险与边界(诚实声明)

1. **登录推断可能误判**(个别网站登录后仍显示"登录"字样)→ 双信号交叉 + 视觉兜底
2. **状态卡 token 成本** → 只放"有意义的信号",目标 <200 字符;超过则精简
3. **像素级内容仍不可见** → 视觉兜底层不变(状态卡覆盖不了的才截图)
4. 内核改动需重新 build all-in-one.js 并同步蓝图版文档

## 六、任务分解(实施顺序)

- [ ] **T1 内核**:`runtime.js` 增加 dialogs/page/forms 状态维护(MutationObserver 增量);`query.js` 加 `getStatus()`;重 build
- [ ] **T2 server**:auth 检测(cookie + DOM 双信号);`_status()` 聚合函数;所有工具返回注入状态卡;`changed` 高亮
- [ ] **T3 测试**:`test_status.py`(上述 5 个场景)
- [ ] **T4 文档**:README 状态卡说明 + 技能包文档更新
- [ ] **T5 git**:提交

## 七、预期收益

- agent 操作网页从"每步猜状态"→"每步看仪表盘",登录/弹窗/表单状态零猜测
- 与 `world_changes`(发生了什么)互补:状态卡=现在是什么,变更流=刚才发生了什么
- 不需要截图即可感知大多数关键状态,视觉层专注像素级疑难

---
**待确认**:方案是否认可?有无要调整的字段/机制?确认后按 T1-T5 实施。